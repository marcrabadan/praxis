{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://praxis/schemas/praxis-config.schema.json",
  "title": "Praxis adapter config",
  "description": "Schema for a consuming repo's .praxis/config.json. Makes agent sessions deterministic: it answers which harness, which project, and which spec are active.",
  "type": "object",
  "required": ["schemaVersion", "harnessRoot", "projectId"],
  "additionalProperties": true,
  "properties": {
    "schemaVersion": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "Semantic version of this config shape (e.g. 1.0.0)."
    },
    "harnessRoot": {
      "type": "string",
      "minLength": 1,
      "description": "Path (relative to the consuming repo) or pointer to the praxis harness root. Must resolve to an existing harness."
    },
    "projectId": {
      "type": "string",
      "pattern": "^[a-z0-9-]{1,64}$",
      "description": "The project this repo belongs to. Must match a folder under the harness's projects/ and that project's PROJECT.md id. If it cannot be resolved, agents stop."
    },
    "mode": {
      "type": "string",
      "enum": ["local", "central"],
      "description": "Optional. 'local' (the default) keeps project memory in this product repo (.praxis/project/); 'central' reads project memory from the harness's projects/<projectId>/. When omitted, treated as 'local'."
    },
    "projectMemory": {
      "type": ["string", "null"],
      "description": "Optional. Explicit path to this project's memory root, overriding the mode default."
    },
    "activeSpec": {
      "type": ["string", "null"],
      "description": "Id or path of the spec currently being worked, or null when none is active."
    },
    "models": {
      "type": "object",
      "description": "Optional. Maps the semantic model tiers praxis commands use to runtime-specific model identifiers (e.g. on Claude Code: haiku/sonnet/opus, or a higher tier like fable for deep; on Codex: the OpenAI models the runtime exposes). When omitted, runtimes fall back to their own defaults. Doctrine: rules/model-tiers.md.",
      "additionalProperties": false,
      "properties": {
        "light": {
          "type": "string",
          "minLength": 1,
          "description": "Model for retrieval/summarization only (context digests). Never a reasoning phase."
        },
        "standard": {
          "type": "string",
          "minLength": 1,
          "description": "Model for the default tier — structured transformation of prior artifacts. Every phase starts here."
        },
        "deep": {
          "type": "string",
          "minLength": 1,
          "description": "Model for frontier reasoning where depth demonstrably pays (hard design, complex root cause, high-stakes domain analysis)."
        }
      }
    },
    "deploy": {
      "type": "object",
      "description": "Optional. Enables the optional, terminal `deploy` step of the feature-development lifecycle: provision/update infrastructure with Terraform and ship to a declared cloud target, driven through an MCP server when configured. Absent or with an empty 'targets' list, the step is skipped and recorded — a cloud target is never assumed. Doctrine: .claude/skills/devops-engineer/references/deploy.md.",
      "additionalProperties": true,
      "properties": {
        "defaultStrategy": {
          "type": "string",
          "enum": ["rolling", "blue-green", "canary"],
          "description": "Default deployment strategy when a target does not override it."
        },
        "guardrails": {
          "type": "object",
          "description": "Optional. Pre-apply gate `deploy-plan-guardrails`: deterministic checks over the terraform plan before promotion. A configured check that fails blocks promotion (fail-closed on production); an unconfigured check is skipped and recorded.",
          "additionalProperties": true,
          "properties": {
            "costBudget": {
              "type": "object",
              "description": "Infracost diff ceiling for the plan's cost delta.",
              "additionalProperties": true,
              "properties": {
                "currency": { "type": "string", "description": "ISO currency code, e.g. USD." },
                "monthlyDelta": { "type": "number", "description": "Maximum allowed increase in projected monthly cost." }
              }
            },
            "policyDir": { "type": "string", "description": "Path to OPA/Conftest policies evaluated over the plan and rendered manifests." },
            "iacScan": { "type": "boolean", "description": "Run an IaC security scanner (tfsec/Checkov) over the plan; block on high-severity findings." }
          }
        },
        "supplyChain": {
          "type": "object",
          "description": "Optional. Pre-apply gate `deploy-supply-chain`: proves the artifact is signed, attested, and SBOM'd before apply. An unsigned/unverifiable image blocks the deploy.",
          "additionalProperties": true,
          "properties": {
            "requireSignedImages": { "type": "boolean", "description": "Verify the image signature (cosign) before apply." },
            "cosignIdentity": { "type": "string", "description": "Expected signer identity (keyless OIDC subject/issuer) cosign must match." },
            "sbom": { "type": "string", "description": "SBOM generator to run and attach to the deploy report, e.g. 'syft'." },
            "requireProvenance": { "type": "boolean", "description": "Require a verified SLSA provenance attestation for the image." }
          }
        },
        "targets": {
          "type": "array",
          "description": "The declared deploy targets. An empty array means the deploy step is skipped.",
          "items": {
            "type": "object",
            "required": ["id", "cloud", "environment"],
            "additionalProperties": true,
            "properties": {
              "id": { "type": "string", "minLength": 1, "description": "Stable id for this target (e.g. prod-eks)." },
              "cloud": { "type": "string", "enum": ["k8s", "aws", "gcp", "azure"], "description": "Target substrate: generic Kubernetes, AWS/EKS, GCP/GKE, or Azure/AKS." },
              "environment": { "type": "string", "minLength": 1, "description": "Environment name (e.g. staging, production)." },
              "mcp": { "type": "string", "description": "Key into deploy.mcpServers naming the MCP server that executes this target. Omit for plan-only." },
              "terraformDir": { "type": "string", "description": "Path to the target's Terraform root, relative to the repo." },
              "strategy": { "type": "string", "enum": ["rolling", "blue-green", "canary"], "description": "Per-target override of defaultStrategy." },
              "promotion": { "type": "string", "enum": ["auto", "manual"], "description": "How promotion happens. Production targets must be 'manual' (explicit human decision)." },
              "slo": {
                "type": "object",
                "description": "Reliability target for the `deploy-healthy` gate: the rollout's error-budget burn over the window must stay within budget.",
                "additionalProperties": true,
                "properties": {
                  "errorBudgetTarget": { "type": "number", "minimum": 0, "maximum": 100, "description": "SLO target as a percentage, e.g. 99.9. The error budget is (100 − target)%." },
                  "burnRateWindow": { "type": "string", "description": "Window over which burn-rate is evaluated, e.g. '1h' or '30m'." }
                }
              }
            },
            "allOf": [
              {
                "description": "A production target must promote manually — an explicit human decision is mandatory, so 'auto' (or an omitted promotion) is rejected for environment 'production'.",
                "if": {
                  "properties": { "environment": { "const": "production" } },
                  "required": ["environment"]
                },
                "then": {
                  "properties": { "promotion": { "const": "manual" } },
                  "required": ["promotion"]
                }
              }
            ]
          }
        },
        "mcpServers": {
          "type": "object",
          "description": "Map of MCP server keys to the server backing them. Referenced by each target's 'mcp'. praxis does not host these servers; they are third-party and hold the cloud credentials — scope each to least privilege for one environment.",
          "additionalProperties": {
            "type": "object",
            "additionalProperties": true,
            "properties": {
              "ref": { "type": "string", "description": "Identifier/source of the MCP server (e.g. hashicorp/terraform-mcp-server)." }
            }
          }
        }
      }
    },
    "generatedBy": {
      "type": "string",
      "description": "Optional. The runtime/tool that generated this adapter (provenance)."
    },
    "praxisVersion": {
      "type": "string",
      "description": "Optional. The praxis version that generated this adapter."
    }
  }
}
