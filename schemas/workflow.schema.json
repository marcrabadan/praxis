{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://praxis/schemas/workflow.schema.json",
  "title": "Workflow manifest",
  "description": "A lightweight, machine-readable lifecycle: ordered steps, the gates that authorize each step, allowed transitions, and stop conditions. Prose belongs in systems/ docs, not here.",
  "type": "object",
  "required": ["id", "name", "steps"],
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^[a-z0-9-]{1,64}$",
      "description": "Lowercase, hyphen-separated workflow id. Must match the file name stem and the registry entry."
    },
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Human-readable workflow name."
    },
    "steps": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "pattern": "^[a-z0-9-]{1,64}$" },
      "description": "Ordered, unique step ids. The lifecycle advances through these in order, subject to gates."
    },
    "gates": {
      "type": "object",
      "description": "Map of step id -> the conditions that must hold before that step may start. Keys must be steps. Conditions are free-form tokens (e.g. 'approved-spec', 'test-evidence') checked by the agent or a validator.",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 }
      }
    },
    "stopConditions": {
      "type": "array",
      "items": { "type": "string", "minLength": 1 },
      "description": "Workflow-local stop conditions, in addition to the global rules/stop-conditions.md."
    },
    "validation": {
      "type": "object",
      "description": "Optional map of step id -> a deterministic command that verifies the step's artifacts.",
      "additionalProperties": { "type": "string" }
    },
    "artifacts": {
      "type": "object",
      "description": "Optional map of step id -> the artifact paths (relative to a spec folder) the step is expected to produce.",
      "additionalProperties": {
        "type": "array",
        "items": { "type": "string", "minLength": 1 }
      }
    },
    "loops": {
      "type": "object",
      "description": "Optional map of step id -> a bounded loop spec for steps that iterate to convergence (notably 'verify' and corrective 'fix'/'change'). Governed by rules/loop-control.md and driven by tools/loop.py: the step runs until its terminal predicate is met (done) or a guard trips (escalate to the user). Keys must be steps.",
      "additionalProperties": {
        "type": "object",
        "required": ["predicate"],
        "additionalProperties": false,
        "properties": {
          "predicate": {
            "type": "array",
            "minItems": 1,
            "items": { "type": "string", "minLength": 1 },
            "description": "The acceptance criteria, all of which must be met for the step to complete. Non-empty: a step with no predicate could loop forever."
          },
          "maxIterations": {
            "type": "integer",
            "minimum": 1,
            "description": "Budget guard for the loop (tools/loop.py applies its default if omitted)."
          },
          "patience": {
            "type": "integer",
            "minimum": 1,
            "description": "No-progress guard: escalate after this many unchanging iterations."
          },
          "onContinue": {
            "type": "string",
            "pattern": "^[a-z0-9-]{1,64}$",
            "description": "The step to return to when an iteration's verdict is 'continue' (the failing predicate is fixed there, e.g. verify -> build). Must be a step in this workflow."
          },
          "gates": {
            "type": "array",
            "items": { "type": "string", "pattern": "^G-[a-z0-9-]{1,48}$" },
            "description": "Optional gate ids (from gateCatalog) this loop's predicate is proven by. Each must resolve to a gateCatalog entry."
          }
        }
      }
    },
    "gateCatalog": {
      "type": "object",
      "description": "Optional catalog of typed verification gates (G-*) that the verify step proves. Distinct from `gates` (which are step entry conditions): these are the deterministic checks a verify report records pass/fail for. A gate may be referenced by a step's loop and by an experience contract's verification list. Keys must match ^G-[a-z0-9-]+.",
      "propertyNames": { "pattern": "^G-[a-z0-9-]{1,48}$" },
      "additionalProperties": {
        "type": "object",
        "required": ["description", "passCriteria"],
        "additionalProperties": false,
        "properties": {
          "description": { "type": "string", "minLength": 1, "description": "What the gate proves." },
          "runner": { "type": "string", "description": "The command that runs the gate, or 'manual' for a manual evidence protocol." },
          "passCriteria": { "type": "string", "minLength": 1, "description": "What counts as pass." },
          "conditional": { "type": "boolean", "description": "True if the gate applies only to some surfaces; when skipped, the verify report must record why." },
          "appliesTo": {
            "type": "array",
            "items": { "type": "string", "enum": ["screen", "flow", "api", "job", "cli", "data", "integration"] },
            "description": "Experience types this gate applies to (for conditional gates)."
          }
        }
      }
    }
  }
}
