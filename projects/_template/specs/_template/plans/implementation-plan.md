# Implementation plan

> Authorized only once the spec is `accepted` (the `approved-spec` gate). Until
> then this is a draft proposal, not a green light to build.

## Approach

The design approach and the key decisions (link `../decisions/` and any ADRs).

## Components & files to touch

- `<path>` — what changes and why.

## Ordered tasks

A high-level ordering; the granular checklist lives in
[`../tasks/tasks.md`](../tasks/tasks.md).

## Risks & assumptions

The top risks and the assumptions the plan rests on.
