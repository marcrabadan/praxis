# Research Report — <Feature title>

> Step 2 of `feature-development`. **Research must precede specification.**
> Routed to `deep-research` (and the relevant domain experts). Reuse > Extend >
> Build: prefer existing solutions and existing praxis skills before proposing
> something new.

## Question(s)

What discovery left open, framed as researchable questions.

## Findings

What the investigation established, each backed by an entry in
[`evidence-log.md`](evidence-log.md). Prefer evidence over assumptions.

## Existing solutions & reusable assets

What already exists (in this codebase, in the org, in the ecosystem, in praxis
skills) that we can reuse or extend instead of building.

## Technical constraints discovered

Constraints found during research that the spec must respect.

## Recommendation

The direction the evidence supports. Alternatives are weighed in
[`alternatives.md`](alternatives.md).

## Traceability

- This artifact id: `RES-<NNN>`
- Sources: discovery (`DISC-<NNN>`)
- Feeds: spec (`SPEC-<NNN>`)
