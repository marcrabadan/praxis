---
id: EV-idea-command
title: "Evidence Log — /idea command"
status: draft
traces:
  part-of: "RES-idea-command"
---

# Evidence Log — /idea command

> Every claim in the research report traces to a row here. One row per source.
> Factual only; opinions belong in the report's recommendations.

| Id | Claim it supports | Source (file:line) | Date | Confidence | Notes |
|----|-------------------|--------------------|------|------------|-------|
| EV-001 | The four categories (`feature`, `bug`, `refinement`, `not-worth-doing`) are sufficient for practical intake — all ten representative inputs map cleanly | `DISC-idea-command` A-1, OQ-2; cross-checked against `.praxis/memory/` ledger history | 2026-06-09 | high | Doc updates → `refinement`; dependency bumps → `refinement`; process improvements → `feature` or `refinement`; ambiguous observations → resolved by 1 clarifying question |
| EV-002 | `refinement` absorbs doc updates, dependency bumps, and process improvements that preserve observable behavior | `refine.md:13–14` ("quality-only work — refactor, performance, tech-debt, readability — that preserves observable behavior") | 2026-06-09 | high | Directly stated in the `refine` Phase 0 gate |
| EV-003 | Inline classification (no skill delegation) is consistent with every existing thin routing command | `.claude/commands/memory.md`, `.claude/commands/patterns.md`, `.claude/commands/learn.md`, `.claude/commands/diagram.md` — none dispatch a subagent for routing logic | 2026-06-09 | high | All four reference commands are 31–41 lines; none use the `Agent` tool |
| EV-004 | Valid `--type` values for `ledger.py log` are exactly: `plan`, `decision`, `implementation`, `artifact`, `test-strategy`, `rollout`, `note` — no `idea` type exists | `.claude/skills/memory/scripts/ledger.py:60` — `TYPES = ("plan", "decision", "implementation", "artifact", "test-strategy", "rollout", "note")` | 2026-06-09 | high | Hardcoded tuple; `cmd_log` at line 374 rejects any value not in `TYPES` with exit code 2 |
| EV-005 | `patterns.py` mines four dimensions — `tag`, `source`, `type`, `stop-condition` — from the ledger; `--source /idea` and `--tags intake,<classification>` provide filterable signal without a new type | `tools/patterns.py:98–126` — `_bump` increments counters for `tag`, `source`, `type`; source and tags are explicitly read from each ledger entry dict | 2026-06-09 | high | No dependency on entry title; `source` field alone distinguishes `/idea` entries from all other commands |
| EV-006 | Thin routing commands share a consistent shape: frontmatter (`description`, `argument-hint`, optional `allowed-tools`), one-paragraph purpose, `$ARGUMENTS`, `## How to handle it` routing section, no subagent | `.claude/commands/memory.md:1–41`, `.claude/commands/patterns.md:1–35`, `.claude/commands/learn.md:1–32`, `.claude/commands/diagram.md:1–32` | 2026-06-09 | high | Establishes the shape constraint for `/idea` (C-5) |
| EV-007 | Auto-routing into `/new-feature` causes double-questioning: `/idea` asks 1–2 Qs, then `/new-feature` Phase 0 asks 2–3 more Qs — same round-trip cost as recommend-and-confirm but without user confirmation of the classification | `new-feature.md:14` ("ask **2–3** clarifying questions with `AskUserQuestion` before starting"); `fix-bug.md:14` ("ask **1–2** focused questions"); `refine.md:12–14` (Phase 0 Frame gate) | 2026-06-09 | high | The two Phase 0 gates ask different questions at different granularity levels (classification vs scoping), so they are complementary rather than redundant — but only recommend-and-confirm avoids back-to-back questioning |
| EV-008 | The `not-worth-doing` category has no routable target lifecycle command; auto-routing produces an inconsistent behavior model (route sometimes, don't route sometimes) | Discovery report `C-6` ("not-worth-doing → ledger capture only, no routing"); confirmed by inspecting all three lifecycle commands — none accept a "not worth doing" input gracefully | 2026-06-09 | high | Auto-routing is logically inconsistent for this path; recommend-and-confirm handles all four outcomes uniformly |
| EV-009 | The `Stop` hook does not duplicate the explicit `log` entry written by `/idea` — it captures working-tree diffs via `snapshot`, not `log` invocations; the `.praxis/memory/**` exclude glob prevents the ledger entry from being re-snapshotted | `integrations/hooks/memory.settings.example.json:20–27` — Stop hook command is `ledger.py snapshot --source auto`; `ledger.py:58` — `MEMORY_EXCLUDE = ":(exclude,glob).praxis/memory/**"`; `ledger.py:402–404` — snapshot returns early when diff is empty | 2026-06-09 | high | `SessionStart` hook (`ledger.py bootstrap --brief` + `pending --brief`) is read-only; neither hook creates a `log` entry |
| EV-010 | `/idea`'s clarification resolves classification ambiguity (new behavior vs quality improvement); `/new-feature` Phase 0 resolves planning scope (target user, desired outcome) — different levels, complementary not redundant | `new-feature.md:14` ("problem, the target user, or the desired outcome is unclear"); `fix-bug.md:14` ("severity and the expected-vs-actual behavior"); `refine.md:13` ("improvement goal and that it is genuinely behavior-preserving") | 2026-06-09 | high | Each Phase 0 gate has a distinct purpose; the argument passed from `/idea` is the clarified one-line summary, which partially pre-answers `/new-feature`'s scope questions |
| EV-011 | Risk R-4 (dismissive "not-worth-doing") requires a one-sentence rationale; risk R-2 (description collision) requires the description to contain "intake and triage" not "plan a feature" | `DISC-idea-command` R-4, R-2 | 2026-06-09 | high | Both risks are structural and must be addressed in the command body and frontmatter description respectively |
| EV-012 | A command file is the correct artifact at this maturity level; no evidence of 5+ independent invocations exists to justify a new top-level skill | `.claude/factory/ai/promotion-policy.md:18` ("5+ distinct sessions for medium-cost promotions"); `DISC-idea-command` C-1 | 2026-06-09 | high | Promotion policy explicitly requires demonstrated independent invocation across sessions before a new skill is warranted |
| EV-013 | `memory.md` has no `allowed-tools` frontmatter entry; `patterns.md` has a narrowly scoped `allowed-tools` for `make patterns` only; `/idea` requires only `Bash` (ledger CLI) and `AskUserQuestion` (clarification) — both default tools | `.claude/commands/memory.md:1–4` (no `allowed-tools` line); `.claude/commands/patterns.md:4` (`allowed-tools: Bash(make patterns), ...`) | 2026-06-09 | high | No `allowed-tools` entry needed for `/idea`; matches assumption A-6 |
| EV-014 | The `--source` and `--tags` parameters in `ledger.py log` accept free-form strings; `--tags` accepts a comma-separated list parsed at line 380 | `ledger.py:379–380` — `tags = [t.strip() for t in (args.tags or "").split(",") if t.strip()]` | 2026-06-09 | high | `--tags "intake,feature"` produces `["intake", "feature"]` in the entry — both filterable by `patterns.py` |
