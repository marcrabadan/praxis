# Alternatives Analysis — <Feature title>

> The options considered during research, weighed against each other. The chosen
> one becomes the spec's direction; significant rejected options become
> `decisions/` ADR notes so the "why not" is traceable.

## Options

### Option A — <name>
- **Summary:**
- **Pros:**
- **Cons:**
- **Reuse posture:** reuse / extend / build

### Option B — <name>
- **Summary:**
- **Pros:**
- **Cons:**
- **Reuse posture:** reuse / extend / build

## Recommendation

Which option and why, in one or two sentences. Record the call as a decision in
`decisions/` (and the memory ledger) if it is significant.

## Traceability

- This artifact id: `ALT-<NNN>` (part of `RES-<NNN>`)
- Feeds: spec (`SPEC-<NNN>`), decisions (`ADR-<NNN>`)
