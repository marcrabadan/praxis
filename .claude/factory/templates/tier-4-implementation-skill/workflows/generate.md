# Generate Workflow

(Replace with the generation procedure. Include explicit inputs, outputs, and validation steps.)

1. Read inputs.
2. Apply transformations.
3. Write outputs.
4. Run `scripts/validate.py` on outputs.
5. Report what was produced.

## Stop condition

(When generation is done and validation passes.)
