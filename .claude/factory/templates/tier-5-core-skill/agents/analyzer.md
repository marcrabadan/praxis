# Analyzer

(Specialist subagent role: reads benchmark results and surfaces patterns aggregate stats hide — flaky evals, non-discriminating assertions, time/token trade-offs. Replace with the actual analyzer prompt.)
