# Grader

(Specialist subagent role: evaluates outputs against assertions in `evals/output-evals.json`. Returns pass/fail with evidence per assertion. Replace with the actual grader prompt for this skill.)
