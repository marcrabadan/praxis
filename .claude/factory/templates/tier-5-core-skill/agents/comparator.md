# Comparator

(Specialist subagent role: receives two outputs without knowing which version produced which, and picks the better one with reasoning. Used for blind A/B between skill versions. Replace with the actual comparator prompt.)
